# mycelium-macros

Procedural macros used by [`mycelium`](https://crates.io/crates/mycelium).

Most applications should depend on `mycelium` directly. It re-exports the
`#[provides]` and `#[consumes]` macros from this implementation crate.
